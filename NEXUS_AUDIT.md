# Nexus audit
Implemented/testable in CI: core, sandboxed files, projects, SQLite read tools, code syntax validation, project context search, tool permissions, basic game generation, SVG assets, resource policy, local API core, local-model adapter, Termux detection, server port probe and validation gate.

Still device/environment gated: Android APK/native UI, native llama.cpp Android runtime, real GGUF inference on target phone, local TTS/STT playback, external voice provider credentials, physical Arduino/ESP32/Pico tests, hardware serial I/O, and remote provider integrations.

These remain BLOCKED/NOT_TESTED until real prerequisites exist. CI success is not treated as proof of Android/hardware functionality.
